var app1 = new Vue({
    el:'#app1',
    methods: {
        slugify: function (string) {
        var minus = this.string.toLowerCase().replace(/\s+/g, "-");
            return alert('alerta');
    }
}
});